return {
  'neovim/nvim-lspconfig',
  config = function()
    local lspconfig = require('lspconfig')
    
    -- Future cross-platform LSP configs go here
    -- Example: lspconfig.lua_ls.setup{}
  end,
}
